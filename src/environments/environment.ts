//Entorno de produccion, Configuramos la url que nos da azure para la web app. solo funciona con ng build

export const environment = {
    production: true,
    apiUrl: 'https://tu-api-en-azure.com' // Reemplaza con la URL real de tu API en Azure
};
