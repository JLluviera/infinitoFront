//Entorno de produccion, configuramos la url que nos da la web app en local. localhost:......
// solo funciona con ng serve

export const environment = {
    production: false,
    apiUrl: 'https://localhost:7078' //En mi caso siempre es este puerto
};
